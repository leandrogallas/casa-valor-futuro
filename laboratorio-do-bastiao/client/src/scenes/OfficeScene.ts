// Cena principal — renderiza o tilemap do escritório, jogadores e agentes.
// Stub: estrutura placeholder até integração com Phaser e Colyseus.
export class OfficeScene {
  static readonly key = 'OfficeScene';

  create(): void {
    // futuro: this.add.tilemap(...), this.physics.add.sprite(...)
    //         this.input.on('pointerdown', ...) etc.
  }

  update(_time: number, _delta: number): void {
    // futuro: tick local + interpolação de estado remoto
  }
}
