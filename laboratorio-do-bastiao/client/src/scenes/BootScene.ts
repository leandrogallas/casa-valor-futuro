// Cena de boot — carrega assets globais (tileset, sprites de avatar, UI).
// Stub: placeholder até Phaser ser adicionado como dependência.
export class BootScene {
  static readonly key = 'BootScene';

  preload(): void {
    // futuro: this.load.image(...), this.load.tilemapTiledJSON(...)
  }

  create(): void {
    // futuro: this.scene.start('OfficeScene');
  }
}
